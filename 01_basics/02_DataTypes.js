"use strict"; // treat all code as newer js version

// alert(3 + 3); // when we will run this here then it will give u werror but when in browser we will write it will give us alert box in browser

let name = "Jay"; // string
let age = 24; // number;
let isloggedin = true; // booleans

// primitive data types

// number => 2 to power 53
// bigint
// string
// boolean => true or false
// null => standalone value
// undefined =>
// symbol => to find uniqness

// object

console.log(typeof "Jay");
console.log(typeof null);
console.log(typeof undefined);

// interview question
// undefined ka type undefined hi he but null ka type ek object he

// typeof undefined is "undefined" because undefined is a primitive type that directly represents an uninitialized state.
// typeof null is "object" due to a legacy bug in JavaScript, which has been kept for backward compatibility.
